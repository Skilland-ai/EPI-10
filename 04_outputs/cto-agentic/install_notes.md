# founder-cto Install Notes

This repo now contains the source skill at:

- `shared/skills/founder-cto/`

The export package is generated at:

- `04_outputs/cto-agentic/founder-cto.skill`

No global installation was performed.

To install later, copy or import `founder-cto.skill` into the target Codex skills location according to that environment's skill installation flow. If installing manually, unpack or import it so the resulting skill folder is named `founder-cto` and contains `SKILL.md`, `agents/openai.yaml`, and the `references/` directory.

After installing globally, verify the skill appears as `founder-cto` and that the default prompt references `$founder-cto`.
